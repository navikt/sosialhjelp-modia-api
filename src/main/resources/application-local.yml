spring:
  redis:
    host: ${REDIS_HOST:localhost}
    port: 1313
    password: ${REDIS_PASSWORD:foobar}

modia:
  cache:
    redis_mocked: true
    time_to_live_seconds: 5

no.nav.security.jwt:
  issuer.veileder:
    discoveryurl: http://metadata
    accepted_audience: aud-localhost
    cookie_name: localhost-idtoken

client:
  abac_pdp_endpoint_url: ${ABAC_PDP_ENDPOINT_URL:abac_pdp_endpoint_url}
  fiks_digisos_endpoint_url: ${FIKS_DIGISOS_ENDPOINT_URL:https://api.fiks.test.ks.no}
  fiks_integrasjonid: ${INTEGRASJONSID_FIKS}
  fiks_integrasjonid_kommune: ${INTEGRASJONSID_FIKS_KOMMUNE}
  fiks_integrasjonpassord: ${INTEGRASJONPASSORD_FIKS}
  fiks_integrasjonpassord_kommune: ${INTEGRASJONPASSORD_FIKS_KOMMUNE}
  norg_endpoint_url: ${NORG_ENDPOINT_URL:norg_endpoint_url}
  pdl_endpoint_url: ${PDL_ENDPOINT_URL:pdl_endpoint_url}
  sts_token_endpoint_url: ${STS_TOKEN_ENDPOINT_URL:sts_token_endpoint_url}

no.nav.sosialhjelp.idporten:
  token_url: ${ID_PORTEN_TOKEN_URL:https://oidc-ver2.difi.no/idporten-oidc-provider/token}
  client_id: ${ID_PORTEN_CLIENT_ID:1c3631f4-dbf2-4c12-bdc4-156cbd53c625}
  scope: ${ID_PORTEN_SCOPE:ks:fiks}
  config_url: ${ID_PORTEN_CONFIG_URL:https://oidc-ver2.difi.no/idporten-oidc-provider/.well-known/openid-configuration}
  truststore_type: ${TRUSTSTORE_TYPE}
  truststore_filepath: ${TRUSTSTORE_FILEPATH}

loginurl: ${LOGINURL:http://localhost:8383/sosialhjelp/modia-api/local/cookie}